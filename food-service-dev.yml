server:
  port: 9098
management:
  endpoints:
    web:
      exposure:
        include: "*"

  info:
    env:
      enabled: true

info:
  app:
    name: "Food Service"
    description: "This is dev phase"
    version: "1.0V"